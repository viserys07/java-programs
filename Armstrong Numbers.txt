class Solution {
    static boolean armstrongNumber(int n) {
        int sum=0;
        int temp=0;
        while(n!=0){
            
            sum+=Math.pow(n%10,3);
            n=n/10;
            
            
        }
        return == temp;
        
        
        
    }
}
